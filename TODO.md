# 로또 발매기 TODO

최종 목표: 로또 발매기 요구사항을 만족하는 콘솔 애플리케이션 구현

0. 예시 상황 성공 하드코딩 (case-test)
  - #0-1 (case-test) 실행 예시 입력(구입금액/당첨번호/보너스번호)을 넣으면 예시 출력 형식과 동일한 결과를 반환한다. ✅
  - #0-2 (case-test) 구입금액이 1,000원 단위가 아니면 `[ERROR]`로 시작하는 `IllegalArgumentException`을 반환한다. ✅
  - #0-3 (case-test) 당첨 번호가 6개가 아니면 `[ERROR]`로 시작하는 `IllegalArgumentException`을 반환한다. ✅
  - #0-4 (case-test) 당첨 번호 6개 중 중복된 숫자가 있으면 `[ERROR]`로 시작하는 `IllegalArgumentException`을 반환한다. ✅
  - #0-5 (case-test) 보너스 번호가 당첨 번호와 중복되면 `[ERROR]`로 시작하는 `IllegalArgumentException`을 반환한다. ✅

1. 구입 금액 검증 모듈 치환
  - #1-1 (unit) 구입 금액이 양의 1,000원 단위인지 검증하는 테스트를 작성한다. ✅
  - #1-2 구입 금액 검증 모듈을 구현한다. ✅
  - #1-3 App이 구입 금액 하드코딩 분기 대신 검증 모듈을 사용하도록 연결한다. ✅

2. 당첨 번호/보너스 파싱 및 검증 치환
  - #2-1 (unit) 당첨 번호 문자열(`1,2,3,4,5,6`) 파싱 테스트를 작성한다. ✅
  - #2-2 당첨 번호 개수(6개) 검증을 테스트와 최소 구현으로 추가한다. ✅
  - #2-3 당첨 번호 범위(1~45) 검증을 테스트와 최소 구현으로 추가한다. ✅
  - #2-4 당첨 번호 중복 금지 검증을 테스트와 최소 구현으로 추가한다. ✅
  - #2-5 App이 당첨 번호 하드코딩 분기 대신 파서를 사용하도록 연결한다. ✅
  - #2-6 보너스 번호 파싱/당첨 번호 중복 금지 검증을 테스트와 최소 구현으로 추가한다. ✅
  - #2-7 App이 보너스 번호 하드코딩 분기 대신 검증기를 사용하도록 연결한다.

3. 로또 발행 모듈 치환
  - #3-1 (unit) 발행된 로또 1장의 번호가 1~45 범위, 중복 없음, 6개인지 테스트한다.
  - #3-2 로또 번호 생성기/로또 도메인 모델을 구현한다.
  - #3-3 (unit) 구입 금액에 따른 발행 장수 계산 테스트를 작성한다.
  - #3-4 발행 장수 계산 및 다건 발행 모듈을 구현한다.
  - #3-5 App이 발행 결과 하드코딩 분기 대신 로또 발행 모듈을 사용하도록 연결한다.

4. 당첨 판정 및 통계 모듈 치환
  - #4-1 (unit) 등수 판정(1등~5등, 미당첨) 테스트를 작성한다.
  - #4-2 당첨 번호/보너스 번호 기준의 등수 판정 모듈을 구현한다.
  - #4-3 (unit) 여러 장 결과의 등수별 집계 테스트를 작성한다.
  - #4-4 등수별 집계 및 당첨금 합계 계산 모듈을 구현한다.
  - #4-5 App이 당첨 통계 하드코딩 분기 대신 판정/집계 모듈을 사용하도록 연결한다.

5. 수익률 계산 및 출력 포맷 치환
  - #5-1 (unit) 수익률 계산 및 소수점 둘째 자리 반올림 테스트를 작성한다.
  - #5-2 수익률 계산 모듈을 구현한다.
  - #5-3 (unit) 통계 출력 문구 포맷 테스트를 작성한다.
  - #5-4 출력 포매터를 구현한다.
  - #5-5 App이 수익률/출력 하드코딩 분기 대신 계산기/포매터를 사용하도록 연결한다.

6. 예외 재입력 흐름 치환
  - #6-1 (case-test) 잘못된 구입 금액 입력 후 같은 단계부터 재입력되는 시나리오를 검증한다.
  - #6-2 (case-test) 잘못된 당첨 번호 입력 후 같은 단계부터 재입력되는 시나리오를 검증한다.
  - #6-3 (case-test) 잘못된 보너스 번호 입력 후 같은 단계부터 재입력되는 시나리오를 검증한다.
  - #6-4 App 실행 흐름을 입력 단계별 재시도 루프로 치환한다.

7. 마무리 정리
  - #7-1 (refactor) 하드코딩 잔여 분기를 제거하고 책임을 명확히 정리한다.
  - #7-2 (refactor) 네이밍/상수/클래스 책임 주석을 규칙에 맞춰 정리한다.
